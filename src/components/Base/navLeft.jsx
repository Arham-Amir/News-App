
const NavLeft = (props = {}) => {
  return (
    <section className={`${props.className}`}>
      <img src="/images/nav_logo.svg" width={110} alt="nav_logo" />
    </section>
  );
}

export default NavLeft;
